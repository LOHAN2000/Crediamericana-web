const API_CONFIG = Object.freeze({
    baseUrl: "https://crediamericana-web.onrender.com/api",
    endpoints: {
        login: "/Auth/login",
        session: "/Auth/session",
        users: "/Users"
    }
});